# Wave 1 Verification Evidence Report (Agent D)
Date: 2026-03-02

## 1) Scope + Commit/Revision Context Reviewed

### Scope verified
- Backend + storefront Wave 1 features:
  1. Product Reviews
  2. Wishlist
  3. Reorder (partial add + unavailable + suggested variants)
  4. First-purchase discount (flag/config; default disabled)
  5. Category image integration
  6. Meta product feed
  7. Abandoned cart admin flow (process + visibility)
- Required auth/ownership matrix checks for protected routes.
- Regression smoke for cart/checkout/account/loyalty/store-category navigation, including responsive checkpoints.

### Revisions reviewed
- Backend repo: `/Users/franciscraven/Desktop/total-hemp/total-hemp-consumables`
  - branch: `main`
  - commit: `d2d440b9a01bf6f7a9a5092d31727cdf10ee7550`
- Storefront repo: `/Users/franciscraven/Desktop/total-hemp/total-hemp-consumables-storefront`
  - branch: `main`
  - commit: `feeb09289a9565abc241fe8d6505169fed1bb069`

### Runtime evidence artifacts used
- `/private/tmp/wave1-api-probe.json`
- `/private/tmp/wave1-storefront-qa.json`
- `/private/tmp/wave1-backend-9001.log`
- `/private/tmp/wave1-responsive-artifacts/20260302-140942/*`
- Targeted rerun captures:
  - `/private/tmp/wave1-rerun-*.txt`

## 2) Doc Map

| Source path/link | Why relevant | Acceptance assertions derived |
|---|---|---|
| `/Users/franciscraven/Desktop/total-hemp/total-hemp-consumables-storefront/docs/wave-1-docs/product-reviews.md` | Canon for review workflow + store route behavior | `GET /store/products/:id/reviews` returns paginated review list; review creation is authenticated; moderation/pending path is surfaced in storefront. |
| `/Users/franciscraven/Desktop/total-hemp/total-hemp-consumables-storefront/docs/wave-1-docs/wishlist.md` | Canon for wishlist module/workflow/route design | Wishlist must persist in module tables, support authenticated add/list/remove flows, and return stable wishlist + item payloads. |
| `/Users/franciscraven/Desktop/total-hemp/total-hemp-consumables-storefront/docs/wave-1-docs/reorder.md` | Canon for reorder API + storefront action | Authenticated customer reorder builds cart, reports unavailable/suggested variants, and hands off cart for checkout. |
| `/Users/franciscraven/Desktop/total-hemp/total-hemp-consumables/docs/wave-1-docs/first-purchase-discount.md` | Canon for first-purchase promotion gating | Discount route returns eligibility/apply state; non-eligible/already-applied/disabled paths are explicit. |
| `/Users/franciscraven/Desktop/total-hemp/total-hemp-consumables-storefront/docs/wave-1-docs/product-category-images.md` | Canon for category media contract | Category image API returns thumbnail/banner/gallery with safe null/empty fallback. |
| `/Users/franciscraven/Desktop/total-hemp/total-hemp-consumables/docs/wave-1-docs/meta-feed.md` | Canon for feed generation + endpoint | Product feed endpoint responds HTTP 200 with valid XML RSS for requested currency/country. |
| `/Users/franciscraven/Desktop/total-hemp/total-hemp-consumables/docs/wave-1-docs/abandoned-cart.md` | Canon for abandoned-cart process behavior | Admin process route supports dry-run vs live semantics; visibility route reflects abandoned-candidate filtering. |
| `/Users/franciscraven/Desktop/total-hemp/total-hemp-consumables/.cursor/skills/building-with-medusa/reference/authentication.md` | Version-local auth behavior reference | `/store/customers/me/*` and `/admin/*` require authenticated caller; ownership checks should be enforced in workflow logic. |
| `/Users/franciscraven/Desktop/total-hemp/total-hemp-consumables/.cursor/skills/building-with-medusa/reference/api-routes.md` | Version-local API route validation patterns | Route-level body/query validation and workflow execution should produce typed, deterministic response contracts. |
| `/Users/franciscraven/Desktop/total-hemp/total-hemp-consumables/.cursor/skills/building-storefronts/references/frontend-integration.md` | Storefront integration constraints | Custom API calls must use `sdk.client.fetch` with plain object bodies; UI should handle loading/error/success states. |
| `/Users/franciscraven/Desktop/total-hemp/total-hemp-consumables/.cursor/skills/storefront-best-practices/reference/components/product-reviews.md` | UX acceptance for review states | PDP reviews should show loading/empty/error/submit states with clear customer messaging. |
| `/Users/franciscraven/Desktop/total-hemp/total-hemp-consumables/.cursor/skills/storefront-best-practices/reference/features/wishlist.md` | UX acceptance for wishlist flows | Wishlist requires auth gate, add/remove feedback, and state consistency after mutation. |
| `/Users/franciscraven/Desktop/total-hemp/total-hemp-consumables/.cursor/skills/storefront-best-practices/reference/mobile-responsiveness.md` | Responsive smoke baseline | Key flows should remain functional/accessible across mobile/tablet/desktop viewports. |
| `/Users/franciscraven/Desktop/total-hemp/total-hemp-consumables-storefront/docs/meta-product-feed-verification.md` | Storefront operator-facing feed check path | Documentation path for manual feed validation must be present and actionable. |

## 3) Commands Executed + Pass/Fail

| Command | Repo | Result | Notes |
|---|---|---|---|
| `yarn build` | backend | PASS | Build completed successfully. |
| `yarn test:unit` | backend | PASS | 3 suites, 18 tests passed. |
| `yarn lint` | storefront | PASS | No ESLint warnings/errors. |
| `yarn build` | storefront | PASS | Next build succeeded. |
| `yarn tsc --noEmit` | storefront | FAIL | `src/components/layout/topbar.tsx:95` type mismatch (`"sm"` not assignable). Classified as pre-existing/unrelated to Wave 1 feature logic. |
| Targeted API curls + probe replay | backend runtime | MIXED | Used `/private/tmp/wave1-api-probe.json` + targeted reruns for contract confirmation. |
| Playwright storefront QA script | storefront runtime | MIXED | Evidence at `/private/tmp/wave1-storefront-qa.json`. |
| Responsive smoke screenshots | storefront runtime | PASS | Required breakpoints present: `390x844`, `1024x768`, `1440x900`. |

## 4) API Contract Verification Table

| Endpoint / scenario | Expected | Observed | Status |
|---|---|---|---|
| `GET /store/products/:id/reviews` | `200`, `{ reviews, count, limit, offset }` | `200`, body matched shape: `{"reviews":[],"count":0,"limit":5,"offset":0}` | PASS |
| `POST /store/products/:id/reviews` (unauth) | auth rejection | `401 Unauthorized` | PASS |
| `POST /store/products/:id/reviews` (owner auth) | workflow result with review payload | `500 unknown_error` | FAIL |
| `GET /store/customers/me/wishlists` (unauth) | auth rejection | `401 Unauthorized` | PASS |
| `GET /store/customers/me/wishlists` (auth) | `{ wishlist: object|null, items: [...] }` | `500 unknown_error` | FAIL |
| `POST /store/customers/me/wishlists/items` (unauth) | auth rejection | `401 Unauthorized` | PASS |
| `POST /store/customers/me/wishlists/items` (auth) | `{ wishlist_id, item }` | `500 unknown_error` | FAIL |
| `DELETE /store/customers/me/wishlists/items/:id` (unauth) | auth rejection | `401 Unauthorized` | PASS |
| `DELETE /store/customers/me/wishlists/items/:id` (auth) | `{ id, removed }` | `500 unknown_error` | FAIL |
| `POST /store/customers/me/orders/:id/reorder` (unauth) | auth rejection | `401 Unauthorized` | PASS |
| `POST /store/customers/me/orders/:id/reorder` (owner) | `{ cart_id, added_items[], unavailable_items[], suggested_variants[] }` | `500 unknown_error` | FAIL |
| `POST /store/customers/me/orders/:id/reorder` (non-owner) | ownership rejection | `500 unknown_error` (no ownership branch observable) | FAIL |
| `POST /store/carts/:id/first-purchase-discount` (unauth) | auth rejection | `401 Unauthorized` | PASS |
| `POST /store/carts/:id/first-purchase-discount` (owner cart) | `{ cart, applied, reason, promotion_code }` | `500 unknown_error` | FAIL |
| `POST /store/carts/:id/first-purchase-discount` (non-owner) | ownership rejection | `500 unknown_error` (no ownership branch observable) | FAIL |
| `GET /store/product-categories/:id/images` | `{ category_id, images:{thumbnail,banner,gallery[]} }` | `200`, contract shape matched | PASS |
| `GET /store/product-categories/:id/images` (missing category) | not found | `404 {"message":"Category not found"}` | PASS |
| `GET /product-feed?currency_code=...&country_code=...` | `200`, `Content-Type: application/xml; charset=utf-8`, valid XML | `500 unknown_error` (JSON error payload) | FAIL |
| `POST /admin/abandoned-carts/process` (unauth) | auth rejection | `401 Unauthorized` | PASS |
| `POST /admin/abandoned-carts/process` (auth dry_run=true) | `processed_count=0` | `500 unknown_error` (from probe artifact) | FAIL |
| `POST /admin/abandoned-carts/process` (auth dry_run=false) | processed count reflects candidates | `500 unknown_error` (from probe artifact) | FAIL |
| `GET /admin/abandoned-carts/visibility` (unauth) | auth rejection | `401 Unauthorized` | PASS |
| `GET /admin/abandoned-carts/visibility` (auth) | filtered abandoned candidates with pagination/count | `200` with `{ carts, count, limit, offset }` and candidate metadata filtering visible | PASS |

### Auth + ownership matrix summary (required)
- Reviews POST auth gate: verified (`401` unauth, `500` on auth path).
- Wishlist auth gate: verified (`401` unauth, `500` on auth path).
- Reorder ownership: blocked by server `500` before owner/non-owner branch result can be validated.
- First-purchase cart ownership/auth: auth gate verified (`401` unauth), ownership behavior blocked by `500` on both owner/non-owner flows.

## 5) Frontend Behavior Matrix (Feature x States Tested x Status)

| Feature | States tested | Status | Evidence |
|---|---|---|---|
| Reviews (PDP) | loading, empty/error messaging, auth gate, submit error path, moderation message code path | PARTIAL/FAIL | UI states exist in `product-reviews` component; auth gate visible. Runtime submit success blocked by backend `500`. |
| Wishlist (PDP) | loading/empty/error, auth gate, add/remove mutation + refetch consistency | FAIL | UI supports optimistic mutation + canonical refetch; backend wishlist endpoints return `500`, so add/remove contract not achievable. |
| Reorder (order details) | loading/error, added/unavailable/suggested rendering, no auto-substitution copy, cart handoff | FAIL | UI contracts implemented; API call fails (`500`), preventing runtime verification of payload rendering/handoff behavior. |
| First-purchase discount (cart summary) | guest auth copy/CTA, button disable, signed-in states, disabled behavior handling | PARTIAL/FAIL | UI state machine implemented; backend endpoint returns `500`, so success/already-applied/not-eligible/disabled server paths not verifiable end-to-end. |
| Category presentation | store category cards, category banner/thumbnail/gallery, null-safe fallback | PASS | `/us/store` and `/us/categories/cbd-tinctures` load 200; media components safely null-guard missing assets. |
| Meta feed visibility docs | docs/check-path present + manual URL check | PARTIAL/FAIL | Docs file present and actionable; manual URL check returns backend `500` instead of XML. |

## 6) Findings by Severity (Critical/High/Medium/Low)

### Critical
1. Workflow-backed Wave 1 API paths are failing at runtime with orchestration crash (`TypeError: Cannot read properties of undefined (reading 'id')`), surfacing `500 unknown_error` on core endpoints.
   - Repro evidence: `/private/tmp/wave1-backend-9001.log` (stack includes `transaction-orchestrator.ts:238`).
   - Affected route call sites:
     - `src/api/store/customers/me/orders/[id]/reorder/route.ts:10`
     - `src/api/store/carts/[id]/first-purchase-discount/route.ts:15`
     - `src/api/product-feed/route.ts:8`
     - `src/api/store/customers/me/wishlists/items/route.ts:12`
     - `src/api/store/customers/me/wishlists/items/[id]/route.ts:10`
     - `src/api/store/products/[id]/reviews/route.ts:76`
   - Impact: No-go. Core Wave 1 mutation/processing paths are non-operational.

2. Wishlist persistence schema is missing in runtime DB (`relation "wishlist" does not exist`), breaking wishlist read/write.
   - Repro evidence: `/private/tmp/wave1-backend-9001.log` with `TableNotFoundException` on `wishlist` query.
   - Relevant schema/migration paths:
     - `src/modules/wishlist/models/wishlist.ts:4`
     - `src/modules/wishlist/models/wishlist-item.ts:4`
     - `src/modules/wishlist/migrations/Migration20260302153524.ts`
   - Impact: Wishlist feature cannot function in deployed runtime until migration state is fixed.

### High
1. Ownership behavior for reorder and first-purchase cannot be validated because requests fail before ownership branch execution.
   - Expected rejection conditions are implemented in workflow steps, but runtime never reaches stable contract response.
   - Relevant logic paths:
     - `src/workflows/reorder/steps/reorder-order.ts:128`
     - `src/workflows/first-purchase/steps/evaluate-first-purchase.ts:70`

2. Meta feed endpoint fails contract (returns JSON error, not XML RSS), blocking external catalog ingestion workflows.
   - Route contract location: `src/api/product-feed/route.ts:15`
   - Workflow entrypoint: `src/workflows/meta-feed/generate-meta-feed.ts:10`

### Medium
1. Abandoned cart process dry-run/live semantics remain unverified end-to-end due process route failure under authenticated probe.
   - Process route: `src/api/admin/abandoned-carts/process/route.ts:9`
   - Workflow entrypoint: `src/workflows/abandoned-cart/process-abandoned-carts.ts:17`
   - Visibility route itself is functioning and returns candidate-filtered count.

### Low
1. Storefront typecheck reports pre-existing unrelated TS error.
   - `src/components/layout/topbar.tsx:95` (`"sm"` not assignable to `"orig" | "md" | undefined`).
   - Does not block `yarn build`, but blocks clean `tsc --noEmit` gate.

## 7) Blockers + Owner Recommendation

### Blockers
- Backend workflow runtime instability (`500 unknown_error` + orchestrator crash) across multiple Wave 1 endpoints.
- Wishlist DB schema mismatch (migration state issue) causing table-not-found runtime failures.
- Abandoned process dry-run/live semantics blocked by endpoint failure.

### Owner recommendations
1. **Backend owner**: triage orchestrator failure first (single root likely affecting multiple workflows), then re-run full contract suite.
2. **Backend/Infra owner**: enforce migration execution for wishlist module in the active runtime DB before QA re-test.
3. **QA owner (Agent D rerun)**: once backend is stable, re-run ownership matrix and abandoned process semantics (`dry_run=true` and `dry_run=false`) with authenticated admin context.

## 8) Residual Risks / Monitoring Notes

- Add runtime alerting for `unknown_error` spikes and explicit alert on `transaction-orchestrator` TypeErrors.
- Add pre-deploy migration guard to fail deployment if module tables (`wishlist`, `wishlist_item`) are absent.
- Add contract tests for Wave 1 routes (status + response shape + auth/ownership matrix) to prevent regression.
- Keep responsive smoke snapshots in CI artifact retention for quick visual diffing.

## 9) Final Gate Recommendation

## **FAIL**

Wave 1 is **no-go** in current state due critical backend runtime failures and missing wishlist persistence schema, which prevent successful execution of key customer/admin workflows and block full acceptance verification.

---

### Notes on patching during QA
- No code patches were applied in either repo during this verification pass.
